#include <stdio.h>
#include <locale.h>

/*Codifique uma função denominada maiorElemento(), sem retorno, que
receba dois parâmetros: sendo o primeiro um vetor inteiro com 10 elementos
(números aleatórios entre 10 e 99) e o segundo uma variável inteira passada
por referência. A função deve encontrar o maior valor do vetor e colocá-lo
dentro do parâmetro recebido por referência. Veja o exemplo a seguir:*/

void maiorElemento(int vetor, int maiorItem);

int main() {
    setlocale(LC_ALL, "Portuguese");


    return 0;
}

void maiorElemento(int vetor, int maiorItem) {
    
}