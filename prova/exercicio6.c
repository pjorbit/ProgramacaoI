#include <stdio.h>
#include <locale.h>

/*Codifique um programa em C que solicite ao usuário uma data contendo
mês e ano. Após o recebimento, indique a qual estação do ano essa data se
refere. Pergunte se deseja continuar a perguntar s-sim n-não.*/

int main() {
    setlocale(LC_ALL, "Portuguese");

    char opcaoUser = 'S';
    int dia, mes, data;

    while (opcaoUser!='N') {
        
    }
    
    return 0;
}